import { RouteObject } from 'react-router-dom';
import mainMenuRoute from '@/pages/index';
import playRoute from '@/pages/play';

const pages: RouteObject[] = [
  mainMenuRoute,
  playRoute
];

export default pages;